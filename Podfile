# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FoodFacts' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FoodFacts
  pod "Parse"
  pod "AlamofireImage"
  pod "Alamofire"
  pod "SideMenu"

  target 'FoodFactsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FoodFactsUITests' do
    # Pods for testing
  end

end
